spring:
  application:
    name: aggregator

server:
  port: 8081

aggregator:
  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/